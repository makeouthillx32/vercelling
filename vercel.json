{
  "builds": [
    { "src": "api/*.mjs", "use": "@vercel/node" },
    { "src": "pages/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/pages/$1" }
  ]
}
